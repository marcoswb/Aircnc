import { Configuration } from 'webpack';
import { InputEnvironment, Arguments, DevConfiguration } from './types';
export default function (env: InputEnvironment, argv?: Arguments): Promise<Configuration | DevConfiguration>;
